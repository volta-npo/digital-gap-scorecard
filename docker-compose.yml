services:
  app:
    build: .
    image: volta-oss/digital-gap-scorecard:local
    ports:
      - "4173:8080"
    environment:
      VOLTA_LOG_LEVEL: info
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
